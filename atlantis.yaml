version: 3
projects:
  - name: api_gateway
    dir: terraform/nonprod/api_gateway
    workflow: terragrunt

  - name: dynamo_db
    dir: terraform/nonprod/dynamodb
    workflow: terragrunt

workflows:
  terragrunt:
  autoplan:
    enabled: true
    when_modified:
      - "./terraform/modules/**/*.tf"
      - "**/*.tf"
      - "**/terragrunt.hcl"
